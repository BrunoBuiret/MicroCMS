{% extends "layout.html.twig" %}

{% block title %}Home{% endblock %}

{% block content %}
{% for article in articles %}
                <article>
                    <h1><a href="/article/{{ article.id }}" class="article-title">{{ article.title }}</a></h1>
                    <p>{{ article.content}}</p>
                </article>
{% endfor %}
{% endblock %}